#include <iostream>
#include <cmath>

using namespace std;

int main() {
    int n;
    cin >> n;
    // cout << sqrt(n) << endl;
    printf("sqrt of %d = %.2f\n", n, sqrt(n));
    printf("%.10f", M_PI);
    return 0;
}